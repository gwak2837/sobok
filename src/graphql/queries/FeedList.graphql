query FeedList($town: NonEmptyString, $option: FeedOptions) {
  feedListByTown(town: $town, option: $option) {
    id
    creationTime
    contents
    imageUrls
    likeCount
    commentCount
    isLiked
    user {
      id
      imageUrl
      nickname
    }
  }
}
