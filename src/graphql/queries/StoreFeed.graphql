query StoreFeed($storeId: ID!) {
  feedListByStore(storeId: $storeId) {
    id
    creationTime
    contents
    imageUrls
    likeCount
    commentCount
    isLiked
    user {
      id
      imageUrl
      nickname
    }
  }
}
