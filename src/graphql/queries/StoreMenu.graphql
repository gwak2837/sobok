query StoreMenu($storeId: ID!, $menuName: NonEmptyString!) {
  menu2(storeId: $storeId, name: $menuName) {
    id
    name
    price
    isSoldOut
    imageUrls
    store {
      id
      name
      imageUrls
    }
    isLiked
    hashtags
  }
}
