import PageHead from 'src/components/PageHead'

const description = ''

export default function MapBucketPage() {
  return (
    <PageHead title="{버킷이름} - 소복" description={description}>
      사용자 버킷 상세 페이지
    </PageHead>
  )
}
